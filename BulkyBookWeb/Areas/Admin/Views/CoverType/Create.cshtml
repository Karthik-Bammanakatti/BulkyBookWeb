@model CoverType

<form method="post">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <div asp-validation-summary="All"></div>
            <h2 class="text-primary">
                Create cover type.
                <hr/>
            </h2>
            <div class="mb-3">
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control"/>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-outline-primary" style="width:150px; margin-left:12px">Create</button>
            <a asp-controller="CoverType" asp-action="Index" class="btn btn-outline-secondary" style="width:150px; margin-left:12px">Back to list.</a>
        </div>
    </div>
</form>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial"/>
    }
}